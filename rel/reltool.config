{sys, [
       {lib_dirs, ["../apps/", "../deps/"]},
       {rel, "riak_pubsub", "1",
        [
         kernel,
         stdlib,
         sasl,
         riak_pubsub
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "riak_pubsub"},
       {profile, embedded},
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},
       {excl_archive_filters, [".*"]},
       {app, sasl, [{incl_cond, include}]},
       {app, riak_pubsub, [{incl_cond, include}]}
      ]}.

{target_dir, "riak_pubsub"}.

{overlay_vars, "vars.config"}.

{overlay, [
           {mkdir, "data/ring"},
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {template, "files/app.config", "etc/app.config"},
           {template, "files/vm.args", "etc/vm.args"},
           {template, "files/riak_pubsub", "bin/riak_pubsub"},
           {template, "files/riak_pubsub-admin", "bin/riak_pubsub-admin"}
           ]}.
